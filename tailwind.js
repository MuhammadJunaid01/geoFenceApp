// tailwind.js
import {create} from 'twrnc';

const tw = create(require(`./tailwind.config.js`)); // Optional: If you have a custom tailwind config
// Or you can use it directly without passing a config

export default tw;
